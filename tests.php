<?php

# When performing a test like:
if ( $name === 'Ashley')
# The === symbol should be used most:
# === Compares the value AND the type (string, integer, boolan etc.)
# == Only compares the data but does NOT check to see if the data type is the same

?>